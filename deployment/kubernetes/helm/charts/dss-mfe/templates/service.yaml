apiVersion: v1
kind: Service
metadata:
  name: {{ include "dss-mfe.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "dss-mfe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dss-mfe.selectorLabels" . | nindent 4 }}
