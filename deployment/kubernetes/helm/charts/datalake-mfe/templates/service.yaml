apiVersion: v1
kind: Service
metadata:
  name: {{ include "datalake-mfe.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "datalake-mfe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "datalake-mfe.selectorLabels" . | nindent 4 }}
