apiVersion: v1
kind: Service
metadata:
  name: {{ include "husura.fullname" . }}
  labels:
    {{- include "husura.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "husura.selectorLabels" . | nindent 4 }}
