apiVersion: v1
kind: Service
metadata:
  name: {{ include "matomo-mfe.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "matomo-mfe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "matomo-mfe.selectorLabels" . | nindent 4 }}
