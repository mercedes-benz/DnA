apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dataentry-be.fullname" . }}-test-connection"
  labels:
    {{- include "dataentry-be.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dataentry-be.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
