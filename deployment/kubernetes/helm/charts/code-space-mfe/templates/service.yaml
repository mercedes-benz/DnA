apiVersion: v1
kind: Service
metadata:
  name: {{ include "code-space-mfe.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "code-space-mfe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "code-space-mfe.selectorLabels" . | nindent 4 }}
