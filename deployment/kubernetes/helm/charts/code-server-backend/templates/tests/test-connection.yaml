apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "code-server-backend.fullname" . }}-test-connection"
  labels:
    {{- include "code-server-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "code-server-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
