version: "2.1"
services:
  hive-metastore:
    image: ${HIVE_METASTORE_IMAGE:-hive_metastore}
    container_name: hive-metastore
    build:
      #context: ./dockerfiles/hive-metastore/Dockerfile
      dockerfile: $PWD/Dockerfile
    