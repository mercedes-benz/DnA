apiVersion: v1
kind: Service
metadata:
  name: {{ include "airflow-service.name" . }}
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  ports:
    - name: api-http
      port: {{ .Values.service.port }}
  selector:
    name: {{ include "airflow-service.name" . }}
