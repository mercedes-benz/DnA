apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "dataentry-mfe.fullname" . }}
  namespace: {{ .Values.namespace }}
spec:
  ingress:
    - ports:
        - port: {{ .Values.containerPort }}
  podSelector:
    matchLabels:
      {{- include "dataentry-mfe.selectorLabels" . | nindent 8 }}
  policyTypes:
    - Ingress
