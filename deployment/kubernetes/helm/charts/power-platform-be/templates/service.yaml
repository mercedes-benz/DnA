apiVersion: v1
kind: Service
metadata:
  name: {{ include "power-platform-be.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "power-platform-be.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "power-platform-be.selectorLabels" . | nindent 4 }}
