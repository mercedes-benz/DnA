apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: trino-backend-role
  namespace: {{ .Values.namespace }}
rules:
  - verbs:
      - '*'
    apiGroups:
      - '*'
    resources:
      - '*'