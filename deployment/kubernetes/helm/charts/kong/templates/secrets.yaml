apiVersion: v1
kind: Secret
metadata:
  name: kong-settings
  namespace:  {{ template "kong.namespace" . }}
type: Opaque
data:
{{- range .Values.kongSettings }}          
  {{ .key }}: {{ .value | b64enc | quote}}
{{- end }}  

