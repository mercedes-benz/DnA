apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fabric-be.fullname" . }}-test-connection"
  labels:
    {{- include "fabric-be.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fabric-be.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
