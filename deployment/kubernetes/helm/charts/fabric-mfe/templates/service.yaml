apiVersion: v1
kind: Service
metadata:
  name: {{ include "fabric-mfe.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "fabric-mfe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fabric-mfe.selectorLabels" . | nindent 4 }}
