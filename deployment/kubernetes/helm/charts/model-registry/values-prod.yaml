namespace: model-registry
envs:
  - name: DNA_URI
    value: ''
  - name: DNA_AUTH_ENABLE
    value: 'true'
  - name: CORS_ORIGIN_URL
    value: ''
  - name: MINIO_MODELS_BUCKETNAME
    value: models
  - name: MINIO_MODELS_PATTERN_PREFIX
    value: ''
  - name: SWAGGER_HEADER_AUTH
    value: ''
  - name: HOST
    value: ''
  - name: CERT_MANAGER
    value: ''
  - name: MINIO_ENDPOINT
    value: ''
  - name: MINIO_ACCESS_KEY
    valueFrom:
      secretKeyRef:
        key: root-user
        name: model-registry
  - name: MINIO_SECRET_KEY
    valueFrom:
      secretKeyRef:
        key: root-password
        name: model-registry
  - name: JWT_SECRET_KEY
    valueFrom:
      secretKeyRef:
        key: jwt.secret.key
        name: model-registry
secret:
  - key: jwt.secret.key
    value: ''
  - key: root-user
    value: ''
  - key: root-password
    value: ''
replicaCount: 2
image:
  repository: registry-emea.app.corpintra.net/dnaplatform/model-registry
  pullPolicy: IfNotPresent
  tag: 1.1.3
imagePullSecrets:
  name: imagepullsecret
nameOverride: ''
fullnameOverride: model-registry
podSecurityContext:
  runAsUser: 1000
serviceAccount:
  annotations: {}
  name: model-registry-sa
podAnnotations: {}
containerPort: 7373
securityContext: {}
service:
  type: ClusterIP
  port: 80
ingress:
  enabled: false
  className: prod-ingress-intranet
  annotations:
    traefik.frontend.rule.type: PathPrefix
    kubernetes.io/ingress.class: traefik
    traefik.ingress.kubernetes.io/router.tls: 'true'
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    cert-manager.io/cluster-issuer: mercedes-benz-acme
    traefik.ingress.kubernetes.io/router.middlewares: default-redirect@kubernetescrd
  hosts:
    - host: models.kfp.dna.app.corpintra.net
      paths:
        - path: /
          pathType: Prefix
  tls:
    - hosts:
        - models.kfp.dna.app.corpintra.net
      secretName: models-tls-secret
resources:
  requests:
    memory: 512Mi
    cpu: 250m
  limits:
    memory: 1000Mi
    cpu: 500m
vaultInjector:
  enable: true
  annotations:
    vault.hashicorp.com/agent-inject: 'true'
    vault.hashicorp.com/role: 'dna_aws_prod_k8_role'
    vault.hashicorp.com/tls-skip-verify: 'true'
    vault.hashicorp.com/agent-pre-populate-only: 'true'
    vault.hashicorp.com/agent-inject-secret-envvar: dna_aws_kv/prod/kubernetesSecret/modelRegistryService
    vault.hashicorp.com/agent-inject-template-envvar: |
      {{- with secret "dna_aws_kv/prod/kubernetesSecret/modelRegistryService" -}}
        {{- range $k, $v := .Data.data }}
            export {{ $k }}="{{ $v }}"
        {{- end }}
      {{- end }}
      {{- with secret "dna_aws_kv/prod/kubernetesSecret/minio" -}}
        {{- range $k, $v := .Data.data }}
            export {{ $k }}="{{ $v }}"
        {{- end }}
      {{- end }}           
  saName: model-registry-sa
  jarName: model-registry-lib*.jar
podDisruptionBudget:
  enabled: true
  minAvailable: 50%