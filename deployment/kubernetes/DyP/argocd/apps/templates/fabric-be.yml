apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  {{- with .Values.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  name: fabric-backend
  namespace: {{ .Values.namespace }}
  labels:
    name: backend
    env: {{ .Values.env }}
    project: dna  
spec:
  destination:
    namespace: fabric
    server: {{ .Values.server | squote }}
  source:
    path: deployment/kubernetes/helm/charts/fabric-be
    repoURL: {{ .Values.repoURL | squote }}
    targetRevision: {{ .Values.branch }}
    helm:
      releaseName: fabric-backend
      valueFiles:
        - values-{{ .Values.env }}.yaml
  project: dna
{{- if .Values.enableSync }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ApplyOutOfSyncOnly=true
      - CreateNamespace=true
{{- end }}
