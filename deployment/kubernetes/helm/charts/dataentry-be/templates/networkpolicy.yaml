apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "dataentry-be.fullname" . }}
  namespace: {{ .Values.namespace }}
spec:
  ingress:
    - ports:
        - port: 8989
  podSelector:
    matchLabels:
      {{- include "dataentry-be.selectorLabels" . | nindent 8 }}
