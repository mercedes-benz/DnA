apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-frontend-service
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  ports:
  - name: api-http
    port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: {{ .Values.global.appName }}frontend