apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  {{- with .Values.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  name: vault
  namespace: {{ .Values.namespace }}
  labels:
    name: service
spec:
  destination:
    namespace: vault
    server: {{ .Values.server | squote }}
  source:
    path: deployment/kubernetes/helm/charts/vault
    repoURL: {{ .Values.repoURL | squote }}
    targetRevision: {{ .Values.branch }}
    helm:
      releaseName: vault
      valueFiles:
        - values-dhc.yaml
  project: dna
{{- if .Values.enableSync }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ApplyOutOfSyncOnly=true
      - CreateNamespace=true
{{- end }}
