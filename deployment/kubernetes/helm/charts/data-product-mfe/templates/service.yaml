apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-product-mfe.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "data-product-mfe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-product-mfe.selectorLabels" . | nindent 4 }}
