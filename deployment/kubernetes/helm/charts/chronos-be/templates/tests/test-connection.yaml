apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chronos-be.fullname" . }}-test-connection"
  labels:
    {{- include "chronos-be.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chronos-be.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
