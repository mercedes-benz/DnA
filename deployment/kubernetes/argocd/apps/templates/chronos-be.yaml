apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  {{- with .Values.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  name: chronos-backend
  namespace: {{ .Values.namespace }}
  labels:
    name: backend  
spec:
  destination:
    namespace: chronos
    server: {{ .Values.server | squote }}
  source:
    path: deployment/helm
    repoURL: https://git.i.mercedes-benz.com/DNA/dna-chronos-backend.git
    targetRevision: {{ .Values.branch }}
    helm:
      releaseName: chronos-backend
      valueFiles:
        - values-{{ .Values.env }}.yaml
  project: dna
{{- if .Values.enableSync }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ApplyOutOfSyncOnly=true
      - CreateNamespace=true
{{- end }}
