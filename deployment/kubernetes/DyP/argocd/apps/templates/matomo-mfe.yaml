apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  {{- with .Values.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  name: matomo-frontend
  namespace: {{ .Values.namespace }}
  labels:
    name: frontend
    env: {{ .Values.env }}
    project: dna  
spec:
  destination:
    namespace: matomo
    server: {{ .Values.server | squote }}
  source:
    path: deployment/kubernetes/helm/charts/matomo-mfe
    repoURL: {{ .Values.repoURL | squote }}
    targetRevision: {{ .Values.branch }}
    helm:
      releaseName: matomo-mfe
      valueFiles:
        - values-{{ .Values.env }}.yaml
  project: dna
{{- if .Values.enableSync }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ApplyOutOfSyncOnly=true
      - CreateNamespace=true
{{- end }}
