apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "husura.fullname" . }}-test-connection"
  labels:
    {{- include "husura.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "husura.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
