apiVersion: v1
kind: Service
metadata:
  name: {{ include "chronos-be.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "chronos-be.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chronos-be.selectorLabels" . | nindent 4 }}
